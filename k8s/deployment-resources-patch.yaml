spec:
  template:
    spec:
      containers:
        - name: my-web-app
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "768Mi"
